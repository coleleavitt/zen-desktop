interface:
  display_name: "Zen Desktop"
  short_description: "Repo-specific patterns and workflows for zen-desktop"
  default_prompt: "Use the zen-desktop repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true